#include<stdio.h>
void main()
{
 int count=0;
 int sum=0;
 int num;
 for (num=101;i<200;i++)
 {
  if ( num%7==0)
  {
   sum+=num;
   count++;
  }
 }
 printf("number of integers divisible by 7 is %d\n",count);
 printf("sum of integers divisible by 7 is %d\n",sum);
}

